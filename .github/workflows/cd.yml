name: CD

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup go
        uses: actions/setup-go@v4

      - name: build app
        working-directory: ./notely

        # build the notely app
        run: ./scripts/buildprud.sh
